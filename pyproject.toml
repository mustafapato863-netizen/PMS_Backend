[project]
name = "pms-dashboard-api"
version = "1.0.0"
dependencies = [
    "fastapi",
    "pydantic",
    "uvicorn",
    "pandas",
    "openpyxl",
    "numpy",
    "python-multipart",
]
requires-python = ">=3.11"