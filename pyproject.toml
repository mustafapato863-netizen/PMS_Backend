[project]
name = "pms-dashboard-api"
version = "1.0.0"
dependencies = [
    "fastapi",
    "pydantic",
    "uvicorn",
    "pandas",
    "openpyxl",
    "numpy",
]
requires-python = ">=3.11"

[tool.uv]
dev-dependencies = ["pywrangler"]